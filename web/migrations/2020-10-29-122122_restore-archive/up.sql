DELETE
FROM ARCHIVE
WHERE EXISTS
    (SELECT *
     FROM
       (SELECT json_object_keys(counts) AS KEY) AS KEY
     WHERE KEY.KEY LIKE '%\n');


INSERT INTO crawled
  (SELECT *
   FROM ARCHIVE);


DROP TABLE ARCHIVE;
