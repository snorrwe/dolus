# Configuring collected words

Will use the first one that's set

1. Set the `WORDS` environmental variable, words separated by `;`'s
2. Set the `words.txt` file with a list of words each in its own line
